import React from 'react'

function Unvalid() {
    // setNewData([
    //     ...newData, { ...data }
    // ]);
    // console.log(newData);
    // alert('Data added successfully');

    // if (data.password.length < 8) {
    //     error.password = "Password must be at least 8 characters long";
    // }

    // if (!/[A-Z]/.test(data.password)) {
    //     error.password = "Password must contain at least one uppercase letter";
    // }

    // if (!/[a-z]/.test(data.password)) {
    //     error.password = "Password must contain at least one lowercase letter";
    // }

    // if (!/\d/.test(data.password)) {
    //     error.password = "Password must contain at least one numeric digit";
    // }

    // if (Object.keys(error).length === 0) {
    //     if (!error.email) {
    //         setNewData([
    //             ...newData, { ...data }
    //         ]);
    //         console.log(newData);
    //     } else {
    //         alert("Please correct the validation errors before submitting.");
    //     }
    // } else {
    //     alert("Please correct the email validation error before submitting.");
    // }

    // if (!error) {
    //     setNewData([
    //         ...newData, { ...data }
    //     ])
    //     console.log(newData);


    // } else {
    //     alert("Please fill all the fields")
    // }

    
    // const deletedata = (id) => {
    //     let freshdata = [...newData];
    //     freshdata.map((item) => {
    //         if (id === item.id) {
    //             freshdata = freshdata.splice(1);

    //         }
    //         setNewData(freshdata)
    //     })
    // }

       // const deletedata = (id) => {
    //     const updatedData = newData.filter(item => item.id !== id);
    //     setNewData(updatedData);
    // }
    return (
        <div>Unvalid</div>
    )
}

export default Unvalid